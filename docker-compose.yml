version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: yantar-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: yantar_dev
      POSTGRES_USER: yantar
      POSTGRES_PASSWORD: yantar_dev_password
    volumes:
      - yantar_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U yantar -d yantar_dev"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  yantar_pgdata:
